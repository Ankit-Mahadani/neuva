# Neuva standard library demo — import stdlib modules and use their helpers.
import metrics
import preprocessing
import visualization

let encoded = one_hot(2, 5)
print "One-hot:", encoded

let losses = [0.9, 0.7, 0.5, 0.3, 0.2, 0.1]
print_loss_curve(losses)
print plot(losses)

let f1 = f1_from_pr(0.8, 0.6)
print "f1_from_pr:", f1
